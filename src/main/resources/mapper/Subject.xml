<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nts.intern.school.subject.dao.SubjectDao">	

	<insert id="addSubject" parameterType="com.nts.intern.school.subject.vo.Subject">
		INSERT INTO subject(id,
		name) VALUES(#{id}, #{name})
	</insert>

	<delete id="deleteSubject" parameterType="Integer">
		DELETE FROM subject
		WHERE id = #{id}
	</delete>

	<update id="modifySubject" parameterType="com.nts.intern.school.subject.vo.Subject">
		UPDATE subject SET
		name
		= #{name} WHERE id = #{id}
	</update>

	<select id="searchSubjects" resultType="com.nts.intern.school.subject.vo.Subject">
		SELECT * FROM subject
	</select>

	<select id="searchSubjectsById" resultType="com.nts.intern.school.subject.vo.Subject">
		SELECT * FROM subject
		WHERE id = #{id}
	</select>

	<select id="searchSubjectsByName" resultType="com.nts.intern.school.subject.vo.Subject">
		SELECT * FROM
		subject WHERE name = #{name}
	</select>

	<select id="isSubjectExistById" resultType="Integer">
		SELECT id FROM
		subject
		WHERE id = #{id}
	</select>

	<select id="getSubjectCount" resultType="Integer">
		SELECT COUNT(1) FROM
		subject
	</select>
	
</mapper>